# Blue version pod with multiple labels
apiVersion: v1
kind: Pod
metadata:
  name: blue-app
  labels:
    env: dev
    application: web-app
    color: blue
spec:
  containers:
  - name: httpd
    image: varunmanik/httpd:blue
    ports:
    - containerPort: 80
