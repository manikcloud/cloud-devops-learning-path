define service {
    use                     generic-service
    host_name               nginx-target
    service_description     HTTP
    check_command           check_http
    check_interval          5
    retry_interval          3
    max_check_attempts      5
    check_period            24x7
    notification_interval   30
    notification_period     24x7
}

define service {
    use                     generic-service
    host_name               apache-target
    service_description     HTTP
    check_command           check_http
    check_interval          5
    retry_interval          3
    max_check_attempts      5
    check_period            24x7
    notification_interval   30
    notification_period     24x7
}
